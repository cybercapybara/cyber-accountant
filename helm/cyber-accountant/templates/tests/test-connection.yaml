apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cyber-accountant.fullname" . }}-test-connection"
  labels:
    {{- include "cyber-accountant.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cyber-accountant.fullname" . }}:{{ .Values.service.httpPort }}/healthz', '-O', '-', '-q']
  restartPolicy: Never
