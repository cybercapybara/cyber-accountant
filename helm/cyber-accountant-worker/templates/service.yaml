apiVersion: v1
kind: Service
metadata:
  name: {{ include "cyber-accountant-worker.fullname" . }}
  labels:
    {{- include "cyber-accountant-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.healthPort }}
      targetPort: health
      protocol: TCP
      name: health
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "cyber-accountant-worker.selectorLabels" . | nindent 4 }}
