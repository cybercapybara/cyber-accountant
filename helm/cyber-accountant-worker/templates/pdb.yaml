{{- if and .Values.pdb.enabled (gt (int .Values.replicaCount) 1) }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "cyber-accountant-worker.fullname" . }}
  labels:
    {{- include "cyber-accountant-worker.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "cyber-accountant-worker.selectorLabels" . | nindent 6 }}
{{- end }}
