# Static labels every HR order prints, and its page margin box.
# The five order kinds each print their own title and their own body; those
# live in the per-fixture files next to the fixture that selects them — and so
# does "ЖСН/ИИН", which only the hire and dismiss branches restate. It was
# template-wide until business_trip and salary_change got fixtures and proved
# it is not.
margin 18mm

Бұйрық №
Приказ №
БСН/БИН
Басшы / Директор
Бұйрықпен таныстым / С приказом ознакомлен(а):

# `kind` selects which block below is printed; it is never printed itself.
unprinted kind
